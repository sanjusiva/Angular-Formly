import { PanelFieldWrapper } from './panel-field-wrapper';

describe('PanelFieldWrapper', () => {
  it('should create an instance', () => {
    expect(new PanelFieldWrapper()).toBeTruthy();
  });
});
