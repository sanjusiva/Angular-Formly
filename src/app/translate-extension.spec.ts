import { TranslateExtension } from './translate-extension';

describe('TranslateExtension', () => {
  it('should create an instance', () => {
    expect(new TranslateExtension()).toBeTruthy();
  });
});
