import { SalutationPresetProvider } from './salutation-preset-provider';

describe('SalutationPresetProvider', () => {
  it('should create an instance', () => {
    expect(new SalutationPresetProvider()).toBeTruthy();
  });
});
